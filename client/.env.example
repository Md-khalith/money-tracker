# Optional: Set this to your deployed backend URL when frontend is hosted separately
# Example: VITE_API_URL=https://api.yourdomain.com
# If omitted or empty, frontend will use relative /api path (same host)
VITE_API_URL=
